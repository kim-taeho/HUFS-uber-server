type Chat {
    id: Int!
    messages: [Message]
    passengerId: Int!
    driverId: Int!
    passenger: User!
    driver:User!
    rideId: Int!
    ride: Ride!
    createdAt: String!
    updatedAt: String
}